#include <stdio.h>
/**
 * main - prints the last digit of the random
 * number is stored in variable n
 * Return: Always 0 (Success)
 */
int main(void)
{
	for (int x = 'A'; x <= 'Z' ; x++)
	{
		putchar(tolower(x));
	}
	return (0);
}
